{
  "name": "influencer-connect",
  "version": "1.0.0",
  "private": true,
  "description": "Influencer Connect Platform Root Workspace",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
